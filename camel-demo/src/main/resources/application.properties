spring.application.name=camel-demo

# LLM 配置（通过环境变量注入）
spring.ai.openai.api-key=${DASHSCOPE_API_KEY}
spring.ai.openai.base-url=${LLM_BASE_URL}
spring.ai.openai.chat.options.model=${LLM_MODEL_ID}

# 禁用 Web 服务器（纯 CLI 应用）
spring.main.web-application-type=none
